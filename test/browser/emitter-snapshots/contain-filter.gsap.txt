// Generated by @zakkster/lite-scrollforge - toGsap
// Do not edit by hand. Regenerate from the source storyboard.

import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
gsap.registerPlugin(ScrollTrigger);

export function attachScrollAnimations() {
    const animations = [];

    // Track 0: .panel
    // Note: `contain` range is approximated (depends on element/viewport size ratio at generation time).
    animations.push(gsap.fromTo(".panel",
        { opacity: 0.2, filter: "blur(8px)", x: -40 },
        {
            opacity: 1,
            filter: "blur(0px)",
            x: 0,
            ease: "power2.inOut",
            scrollTrigger: {
                trigger: ".panel",
                start: "100% 100%",
                end: "0% 0%",
                scrub: true
            }
        }));

    return function detach() {
        for (let i = 0; i < animations.length; i++) {
            const anim = animations[i];
            if (anim && anim.scrollTrigger) anim.scrollTrigger.kill();
        }
    };
}